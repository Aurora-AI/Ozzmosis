# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Builds
dist/
build/
.next/
out/
*.tsbuildinfo

# Turborepo cache (se existir)
.turbo/

# Runtime artifacts (Trustware: side-effects opt-in)
.artifacts/
last-run.json
artifacts/

# Default: ignore json artifacts
*.json

# Antigravity: allow MCP example template (not real secrets)
!.antigravity/mcp/servers.example.json

# SSOT exception: Vault run artifacts must be versioned
!apps/ozzmosis/data/vault/**/_runs/**/*.json
!apps/ozzmosis/data/vault/**/_runs/**/*.txt
!apps/ozzmosis/data/vault/**/_runs/**/*.md

# Vault (Rodobens transcripts): never commit real outputs
apps/ozzmosis/data/vault/rodobens/trainings/transcripts/_runs/**
!apps/ozzmosis/data/vault/rodobens/trainings/transcripts/_runs/.gitkeep
apps/ozzmosis/data/vault/rodobens/trainings/transcripts/**/*.srt
apps/ozzmosis/data/vault/rodobens/trainings/transcripts/**/*.vtt
apps/ozzmosis/data/vault/rodobens/trainings/transcripts/**/*_transcript.json

# Env files (never commit secrets)
.env
.env.local
.env.*.local
**/.env.triad
**/.env
**/.env.local
**/.env.*.local
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/

# Local type-checker configs (IDE-only)
pyrightconfig.json
apps/crm-core/pyrightconfig.json

# Local audits (not SSOT unless explicitly committed)
docs/AUDITS/

# OS/editor
.DS_Store
Thumbs.db
.vscode/
!.vscode/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
.idea/
.agent/
