# Token de acesso ao Shield (usado pelo Chronos e por testes locais)
SHIELD_TOKEN=local-dev-token

# Opcional: se quiser que o Shield faça proxy real para um upstream externo
# SHIELD_UPSTREAM_URL=https://seu-upstream.exemplo
