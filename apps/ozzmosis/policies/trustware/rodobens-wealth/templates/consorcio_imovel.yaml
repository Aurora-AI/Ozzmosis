version: 1
product: consorcio_imovel
rules:
  - id: require_identity
    type: required_fields
    fields: ["cpf_cnpj", "whatsapp_id"]
  - id: stage_gate_proposal_requires_lifemap
    type: requires
    field: "life_map"
    when_stage: "PROPOSAL"
